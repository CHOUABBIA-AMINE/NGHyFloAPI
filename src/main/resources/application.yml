spring:
  application:
    name: nghyflo-api
  profiles:
    default: dev

server:
  servlet:
    context-path: /

management:
  endpoints:
    web:
      base-path: /actuator

nghyflo:
  api:
    base-path: /nghyflo/api/v1
  security:
    require-external-secrets: false
# TODO
