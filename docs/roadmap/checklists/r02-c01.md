# Checklist — R02-C01

## Roadmap item ID

- `R02-C01`

## Roadmap source file

- `docs/roadmap/phase-02-boundary-normalization.md`

## Objective

- Canonicalize organization-owned `EmployeeId` and `StructureId` usage by
  switching production references away from shared-kernel variants and marking
  the shared variants deprecated for later shared-kernel contraction.

## Preconditions

- [x] `AGENTS.md` was read first.
- [x] `docs/architecture/CODEX-EXECUTION-PLAN.md` identifies `R02-C01` as the
      next Ready item.
- [x] `docs/roadmap/phase-02-boundary-normalization.md` was reviewed.
- [x] Baseline docs (`docs/adr/`, `docs/api/`) are not modified.
- [x] Scope is limited to one roadmap item (`R02-C01`).

## Files expected to change

- `src/main/java/dz/sh/trc/nghyflo/modules/organization/**`
- `src/main/java/dz/sh/trc/nghyflo/platform/security/authentication/**`
- `src/main/java/dz/sh/trc/nghyflo/shared/domain/value/EmployeeId.java`
- `src/main/java/dz/sh/trc/nghyflo/shared/domain/value/StructureId.java`
- Targeted tests that instantiate affected identifier types
- `docs/architecture/CODEX-EXECUTION-PLAN.md`
- `docs/roadmap/checklists/r02-c01.md`

## Implementation checklist

- [x] Replace production shared `EmployeeId` references with organization-owned
      `EmployeeId` where actor/organization flows require employee identity.
- [x] Replace production shared `StructureId` references with organization-owned
      `StructureId` in organization domain/application/API/infrastructure flows.
- [x] Mark shared `EmployeeId` and `StructureId` variants deprecated.
- [x] Update targeted tests to use organization-owned identifiers where they
      exercise organization/platform actor flows.
- [x] Keep shared identifier compatibility tests limited to the deprecated shared
      variants until removal is scheduled by a later roadmap item.

## Coding standards checklist

- [x] Touched Java files checked for mandatory headers where present or added.
- [x] Touched files checked for constructor-injection/field-injection impact.
- [x] Touched files checked for package/import consistency.
- [x] No controller-to-repository or domain-to-infrastructure dependency was
      introduced.
- [x] No unrelated coding-standard cleanup was performed.

## Documentation checklist

- [x] Update `docs/architecture/CODEX-EXECUTION-PLAN.md` with `R02-C01` status,
      current step, next step, validation result, and risks.
- [x] Keep baseline vs generated documentation distinction unchanged.
- [x] Record out-of-scope test compilation failures without expanding commit
      scope.

## Validation checklist

- [x] Run `mvn -q -DskipTests compile`.
- [x] Run `mvn -q test` for unit and architecture tests.
- [x] Run `mvn -q checkstyle:check` for touched Java style guardrails.
- [x] Run `rg` checks to verify no production references remain to shared
      `EmployeeId`/`StructureId` variants.
- [x] Record validation command outputs in
      `docs/architecture/CODEX-EXECUTION-PLAN.md`.

## Closure checklist

- [x] Confirm exactly one roadmap item implemented.
- [x] Confirm no unrelated cleanup or feature work.
- [x] Confirm baseline docs (`docs/adr/`, `docs/api/`) untouched.
- [x] Confirm PR readiness evidence is documented.

## Current step

- `R02-C01` implementation completed; production references now use
  organization-owned identifiers and shared variants are deprecated.

## Next step

- Resolve or explicitly disposition out-of-scope test compilation failures before
  proceeding to `R02-C02`.

## PR readiness criteria

- `R02-C01` implementation is complete.
- Production shared identifier references are removed.
- Compile validation passes.
- Unit/architecture test validation must pass before PR readiness; current run is
  blocked by out-of-scope test compilation failures.
